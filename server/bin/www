const http = require('http');
const app = require('../../app.js');

const port = parseInt(process.env.PORT, 10) || 8000 || 8001;
app.set('port', port);

const server = http.createServer(app);
server.listen(port, () => {
  console.log("Listening on port " + port);
});

