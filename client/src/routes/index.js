import HomePage from '../views/HomePage/HomePage';

const Routes = [
  // PUBLIC ROUTES
  {
    path: '/',
    name: 'Homepage Route',
    component: HomePage,
    exact: true
  }
];

export default Routes;
